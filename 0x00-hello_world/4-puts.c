#include <stdio.h>
/**
 * main - first function in the program
 * puts - The puts function proceeds to write the contents of the strings
 * Return: if success then return 0
 */
int main(void)
{
char str1[] = "Programming is like building a multilingual puzzle";
puts(str1);
return (0);
}
