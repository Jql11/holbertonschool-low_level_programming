#include <stdio.h>
/**
 * main - first function in the program
 * Return: if success then return 0
 */

int main(void)
{
	int i = 48;

	for (i = 48; i <= 56; i++)
	{
		putchar(i);
	putchar(',');
	putchar(' ');
	}
	if (i == 57)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
